<%= form_for @camp_activity do |f|%>
	<%= f.label :name  , "Name"%>
	<%= f.text_field :name%>
	
	<%= f.collection_check_boxes(:camp_worker_ids, CampWorker.all, :id, :name)%>
	<%= f.submit "Update" %>
<% end%>
