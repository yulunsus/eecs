<table class="table table-hover">
<tr>
<th><%= t('Point get when uploading a feedback') %></th>
<td><%= @adminparam.point_get_feedback %></td>
</tr>
<tr>
<th><%= t('Point get when uploading a pastexam') %></th>
<td><%= @adminparam.point_get_pastexam %></td>
</tr>
<tr>
<th><%= t('Point needed when browsing feedbacks') %></th>
<td><%= @adminparam.point_need_feedback %></td>
</tr>
<tr>
<th><%= t('Point needed when downloading a pastexam') %></th>
<td><%= @adminparam.point_need_pastexam %></td>
</tr>
</table>
<% if current_user.is_admin >0 %>
<%= link_to t("Edit") , edit_adminparams_path(@adminparam), :class => "btn btn-info"%>
<% end %>
